<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quizine"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Quizine">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

<!--singleton (a class with a single instance)
-> class -> object keyword like a class with set of constants
    a class without members and only companion object is equivalent to a singleton class.

    static -> anything that does not require object creation for that class are defined as static here companion object
    [members in class]

    by -> we dont directly assign a value to a variable inst6ead we refer it to a delegate which provide it with setter and getter methods.

   init -> initialize an object, its body of constructor of class
    == -> for value and === -> for memory location

    lambda functions-> pass function as parameter to another function
-->